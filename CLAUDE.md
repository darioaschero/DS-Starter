# DS-Starter

CSS-only design system starter. Hand-authored CSS is the source of truth: no build step, no TypeScript descriptors, no token pipeline, no framework runtime, no component library implementation.

## Read before any change

1. [docs/conventions.md](docs/conventions.md) — **normative**. Naming, layers, component pattern, process rules. Every task must follow it.
2. [docs/direction.md](docs/direction.md) — the full direction document (rationale, decision status, deferred decisions).

## Quick facts

- All starter CSS lives in named cascade layers (`ds.*`). Layer order is declared **only** in `ds/core.css` (content-only entry); `ds/index.css` composes core + optional component modules. Ordinary unlayered consumer CSS must win over the starter. No `!important`.
- Component identity: `data-ds-ui="<component>"`. Parts: `data-ds-part="<part>"`. Finite configuration: per-component `data-ds-*` axes (always compounded with identity). Theme switch: `data-ds-theme`. Styling values: `--ds-*` custom properties. State hooks are anatomical paths (conventions §7).
- Preview: `python3 -m http.server <port>` from the repo root, then open `/fixtures/`. Zero build.
- Every work task ends with a findings note in `docs/findings/` (copy `TEMPLATE.md`). The prototype exists to answer questions; findings notes are a first-class deliverable.
- Work tasks are defined as self-contained briefs in `docs/tasks/`, launched by the user as separate sessions. If this session was started from such a brief, follow the brief exactly.
- Tasks do **not** commit. The coordinator session handles git.

## Current status

Milestone 1 (vertical slice) is **complete and committed**: foundations, button, card, rich text, composition + consumer-override fixtures, synthesis in `docs/findings/m1-synthesis.md`. Conventions are at v3 (colour architecture).

T6 (colour architecture v3) and the three M2 component tasks — T7 field, T8 disclosure, T9 stack — are **complete and committed**. Conventions are at v4 (M2 rules promoted: native-state channels, public-vs-state precedence, focus relocation, authoritative state, transparent-bg derived states, finite geometry axes, layout doctrine, relationship rules outside scope).

**Milestone 2 is complete and committed** (T10 integration included): all six direction-§18 components composed and verified on the full fixture, embedded-component rhythm owned by rich-text, `--ds-bg-component` adopted (disclosure content only), M2 synthesis + 30-rule linter inventory in `docs/findings/m2-synthesis.md`.

**Milestone 3 is complete and committed.** Both derivation experiments (T11 LGC-like on `derivation/lgc`, T12 warm editorial on `derivation/editorial` — worktrees `../DS-Starter-lgc`, `../DS-Starter-editorial`) proved the starter thesis with zero frozen-surface leaks. The synthesis is `docs/findings/m3-synthesis.md` (two-path comparison, 15-knob parameter surface, constraint classes, wizard questionnaire, conventions v5 proposals, linter rules 31–34); the shippable derivation guide is `docs/deriving.md`.

**Project thesis v2 (2026-09-01 pivot, user-decided):** DS-Starter becomes a **minimal core + optional modules** that a wizard integrates step by step, with scales and palettes produced by **generative rules** rather than hand-picked values. Core minimo (user's choice): pure content — foundations, generated scales, semantic roles, typography, rich text; **zero components** (the first module will be `actions`/button). T14 base curation is **superseded — do not launch**; its useful pieces return as gates/modules.

**Process rule (conventions §11): design curation belongs to the user.** Research/design tasks deliver options with rendered specimen pages; the user decides at coordinator checkpoints; conventions record the user's choices.

**Palette generation is decided** (user decision 2026-09-01, recorded in conventions §5a): hybrid — the vendored pinned Radix custom-colour generator produces the scales; DS-Starter's own WCAG gates decide on-solid, link/focus steps, and warnings. Research trail: T15 (`findings/palette-rule.md`) and T16 (`findings/radix-generator.md`); engine + three-branch specimen in `docs/research/palette-rule/`.

**T17 (scale rules) is complete and committed** (`findings/scale-rules.md` + live specimen in `docs/research/scale-rules/`): the parametrization space is mapped — spacing progressions × three density models, type ratio + the rounding-order discovery (current type scale = 1.2 with cumulative whole-pixel rounding), leading formula profiles, radius triples, relational gates, breakage corners. **Its 10 user decisions are deliberately deferred**: the research stands as the map for a later parametrization phase, not as a blocker.

**M4 (tight-core curation) is defined — user decisions 2026-09-01, recorded in conventions §12.** The tight core is the content-only subset at file granularity: reset + focus baseline, `scale.css`, `palette.css` (3-family structure), `semantic.css`, `roles.css`, and `rich-text.css` extended toward full content coverage. Components, `recipes.css`, and component-facing tokens stay **dormant in-tree** as the dowry of future modules. Decisions: (1) **double entry** — a new `ds/core.css` owns the cascade-layer declaration and imports only core files; `ds/index.css` composes core + component modules (implemented by T18); (2) **vendored self-hosted open-licensed fonts are admissible as curation options** (zero network, documented licenses, rigorous fallbacks) — the actual typeface choice is the user's, in C1; (3) **colour identity is decided in C4 by rendered comparison** — current Radix verbatim vs custom seeds through the decided hybrid engine; (4) **M5 (expressiveness) = curated composition batteries**: additional content patterns/elements composed only from core scales (measured consumer CSS, no new tokens) to probe the expressive range of the type and spacing scales — identity derivations and docs dogfooding are parked for M5, not adopted.

**Curation cycles (M4):** C0 perimeter + canonical corpus + core entry (**T18 — complete and committed**, `8e180fc`) · C1 typographic voice · C2 type scale / leading / measure · C3 prose rhythm · C4 colour identity + link/focus treatment · C5 extended element coverage (figure, kbd, mark, task lists, footnotes, …) · C6 finishing details (selection, text-wrap, hyphenation, list markers). Cycle mechanics (conventions §12): coordinator brief → specimen task renders 2–4 labelled option **bundles** on the canonical corpus (both schemes + narrow viewport, T17-style root overrides, zero `ds/` edits) → user decides at a checkpoint (mixes allowed) → small landing task writes values + findings → coordinator commits and records the decision. T17's 10 decisions stay deferred; the refined core becomes the fitted instance that later parametrization must reproduce.

**T18/C0 checkpoint outcome (user, 2026-09-01) — the three perimeter calls are OPEN, not decided.** The user declined the core-vs-dowry framing as premature filing of an unstudied vocabulary, and set an exploratory direction instead: **everything colour-related is a context palette that can be overridden** (danger is "perhaps a context"); the semantic colour vocabulary should be studied through that lens before any ownership record. Typography-role naming (label included) is likewise **under study, not validated** — it gets confronted where it is rendered (C1/C2), not filed. Nothing moves at file granularity meanwhile (the dormant tokens already ride along harmlessly). **C1 process change (user):** the user defines the initial typeface set themself; the C1 task renders that set; how further options get authored is a question for later tasks.

**Coordinator handoff notes:** this file + `docs/conventions.md` + the `docs/findings/*-synthesis.md` files + the latest briefs are the complete coordination state. Worktrees: `../DS-Starter-lgc` (branch `derivation/lgc`) and `../DS-Starter-editorial` (`derivation/editorial`) — long-lived reference branches, never merged. Parked threads: the wizard-design questions inside the conventions §5a decision block; the linter rule inventory (m2-synthesis §4 + m3-synthesis rules 31–34); T14 (superseded); identity derivations + docs dogfooding as expressiveness tests (parked at the M4 checkpoint in favour of composition batteries). Process constants: tasks are launched by the user from `docs/tasks/` briefs; tasks never commit (coordinator commits and pushes after review); design curation belongs to the user (conventions §11). Open threads after the T18 checkpoint: the colour-context study (how the "overridable context palette" idea maps onto the semantic tier — study before any vocabulary ownership record); role-naming review inside C1/C2. **The user's typestyle set (2026-09-01, reasoning in progress):** styles named **sm / md / lg** (user decision); working face **Inter** (user, "for now") — InterVariable 4.1 vendored with pinned provenance at `docs/research/typestyles/vendor/inter/` (SIL OFL; sha256 in its README; opsz+wght axes, so optical sizing is active in judgments). Proportions SF Pro / Inter-like. **Rule under refinement (user, 2026-09-01): one constant ratio per family, font sizes adjusted slightly so line-heights land exactly on the 4px grid** (inverts the earlier snap-the-lh reading). Coordinator-derived candidate numbers awaiting the user's eye: text family r = 10/7 (size = 0.7 × lh): **sm 14/20 (fixed) · md 16.8/24 · fluid max 19.6/28**; lg family r = 6/5 (size = lh/1.2): **lg 26.667/32 · fluid max 30/36 — weight NORMAL (user: not semibold)** (alternative keep-26.25 recorded, not chosen). Consequence on record: per-style fluid growth becomes the grid-step ratio (7/6 text, 9/8 heading) — the original shared ×1.153 ramp is superseded (incompatible with constant ratio + both endpoints on grid); hierarchy ratio drifts 1.587 → 1.531 along the ramp (~3.6%). The grid holds at ramp endpoints and in fixed styles; clamp() is continuous mid-ramp. **Fluid model (reasoned 2026-09-01): `em`-derived heading sizes are ruled out (position-dependent, compounds under nesting, breaks the tuple model and the grid — §5b rem doctrine stands); instead ONE shared progress definition (0→1 over a width interval) with per-style rem spans generated by the family rule — heading shares the body's timing, not its size; both lh spans cross exactly one 4px grid step (Δ0.25rem); the same progress variable can later drive tracking. Implementation form to verify in-specimen: unitless length-division in calc() on the 2025 baseline; fallback = per-style clamp(min, calc(A·rem + B·vw), max) with A/B generated from the same recorded interval. Still open: the interval itself (which widths; viewport vs container measure).** **Scratch specimen live (user said "procediamo"): `docs/research/typestyles/specimen.html`** (coordinator-built, zero `ds/` changes) — tuple styles on the shared progress, slider + live-viewport mode with editable interval, tracking on/off + per-style tracking sliders (Figma defaults as reference, reset button), the opsz policy switch (auto / none / pin 14), weight toggles, polarity surfaces with a wght-delta slider, anchored p=0/p=1 rows, live readouts (weight column included). Verified: endpoints exact (16.8/24→19.6/28, 26.667/32→30/36, tracking %), family ratios exact at EVERY p (ray property, e.g. 18.2/26 at p=0.5), typed length-division probe SUPPORTED (parse + computed) on the current browser. **Architectural constraint discovered while building: fluid tuple formulas must resolve at the consumption point (element-level rules) — declared as pre-resolved `:root` tokens, `var(--p)` substitutes at root and subtree re-pinning of `--p` dies.** **THE TYPESTYLE WORKING SET IS USER-APPROVED (2026-09-02)** — full decision block recorded in conventions §12 (sizes/lh + generation rule, tracking, fluid model, opsz auto, antialiased global, polarity −20, Inter). **Landing gates before it enters `ds/`: the ramp interval and the h1–h3 heading mapping; then a landing brief (next task) carries it into the core with the §5b/§11 rewrites.** **Tracking (2026-09-01; % of own size, written as em on letter-spacing — self-relative, outside the em ban). VALUES UNDER REVISION (user):** the Figma-tested numbers read very differently in the browser — lg feels over-corrected (suspected double correction: Figma's static Inter is the text cut, while the browser's InterVariable applies opsz automatically, so lg is already tighter by design before the manual −2%), and semibold appears to want WIDER tracking than book (browser AA renders heavier weights denser; wider-when-bolder is known practice — the class rule survives with an inverted sign). Values are re-judged by eye in the specimen (per-style sliders, Figma defaults kept as reference; judge base values in LIGHT on positive surfaces — dark belongs to the polarity delta). **Judge-base decisions: (1) optical policy — CLOSED by the set approval: opsz AUTO. (2) smoothing policy — DECIDED (user, 2026-09-02): `-webkit-font-smoothing: antialiased` + `-moz-osx-font-smoothing: grayscale` GLOBAL.** User rationale: optimize for mac users, probably the most attentive audience. Coordinator note on record: the property pair is a macOS-only lever (ignored on Android/iOS/Windows/Linux); Android's native rasterization (grayscale, no macOS dilation) already sits close to the antialiased look, so this choice CONVERGES mac with mobile rendering; Windows/ClearType remains the divergent platform with no CSS lever (spot-check later on a real device — browser emulation does not change the rasterizer). The specimen keeps the three-way switch for comparison; Figma's renderer ≈ antialiased, a measurable slice of the semibold-density gap. Figma reference values: sm book (reg/med) 0 (the user's "−", to confirm) · sm semibold −0.5% · md book −0.5% → −1% at fluid max · md semibold −1% → −1.5% · lg −2% → −2.5% at weight normal (the user's lg-normal decision closed the weight TBD). Rules extracted: (1) weight CLASSES — book (regular/medium collapsed) vs semibold = book − 0.5pp; (2) every fluid style tightens uniformly by −0.5pp across the ramp → one system-wide rule `− 0.005em × var(progress)` on the shared progress variable, composing with the fluid size (px = fluid size × fluid pct); (3) per-family rest anchors — text steps −0.5pp per grid step from sm = 0, lg anchored at −2%. **APPROVED VALUES (user "ok approvato", 2026-09-02, after the Android check; judged under opsz auto + antialiased):** rest sm book −0.75% · sm semi −0.5% · md book −1.25% · md semi −0.75% · lg −0.4%, with the uniform −0.5pp fluid tightening on top. The class delta resolved by approval: tracking is freely curated per style (the Figma-era uniform class rule is retired); lg −0.4 vs Figma −2 confirmed the opsz double-correction diagnosis; p=0 and p=1 were both visible via the anchored rows. The font-only role invariant is now formally broken: style = tuple (size, lh, weight-class, tracking) — direction §6 coordinated sidecars confirmed as the model. Start from these three styles only, then build rich text and buttons as their first consumers (buttons re-enter early as type consumers, not as the actions module). **Every spacing / vertical-rhythm decision requires explicit user agreement** — tasks propose and render, never land rhythm values alone. **Polarity correction (user idea, 2026-09-01; user OK'd the specimen demo — live and verified: neg strip 380/580 at delta −20, nested positive box resets to 400 proving set-not-accumulate, black pill in light self-corrects to 580):** scalable font-weight reduction on NEGATIVE surfaces (light text on dark), keyed to surface polarity rather than the active scheme — a white label on a black button in light mode gets it too. Discussed model: polarity is a declared context attribute (surfaces declare, never accumulate; a light surface inside dark re-declares positive), consumed where styles resolve weight (base weight + inherited delta, per the consumption-point rule); viable via InterVariable's continuous wght (small, ≈5–10%; one global delta first, per-style only on rendered evidence). Honest limits on record: wght moves advances (micro-reflow between polarities; GRAD is the purpose-built zero-reflow axis but Inter lacks it; macOS font-smoothing is rendering-only), polarity cannot be derived from the bg colour in CSS today (declared, not computed), light-dark() carries colours only (the system-dark hook for a number is a wiring detail), non-variable fallbacks quantize the delta away. Possible second knob: a polarity tracking delta (near-zero reflow). **DECIDED (user, 2026-09-02): polarity wght delta = −20 on negative surfaces.** This feeds the colour-context study: contexts carry more than colour (palette + polarity + typographic corrections).

**External API review on record (2026-09-02): `docs/reviews/api-review-2026-09.md`** — an in-depth review of the API direction, recorded verbatim with the former coordinator's evidence-checked triage. **Two user decisions taken on it; live-coordinator scheduling (2026-09-02):** (1) **namespace rename approved** — `data-ui`/`data-part`/`data-<axis>` → `data-ds-ui`/`data-ds-part`/`data-ds-<axis>`, **scheduled FIRST as T19 (brief ready: `docs/tasks/T19-namespace-state-reach.md`)** — the tree is quiet (typestyles live only in research), so renaming now means the typestyle landing writes the new namespace instead of being churned twice; T19 also carries the state-reach fix (same mechanical battery) and includes `data-theme` → `data-ds-theme` for API coherence (user may veto at launch). (2) **MUST/MUST-NOT normative contract + executable conformance-fixture suite confirmed** as a roadmap workstream before any wizard (four-concept containment split: cascade governance / selector ownership / value inheritance / state reach) — scheduled as its own task AFTER T19 (so it documents the final namespace and absorbs T19's new probes); it gates the wizard, not the C-cycles. Sequence: **T19 COMPLETE AND COMMITTED** (`8816eaf`: 633→0 legacy occurrences across 19 live files, field's 7 state hooks → 4 anatomical `:is()` paths, nested-state probe proves the fix, battery byte-identical both schemes; findings `namespace-state-reach.md`; conventions promoted to **v6** — `data-ds-*` protocol, axis-compound invariant, §7 state-reach rule) → **landing gates CLOSED (user, 2026-09-02, recorded in conventions §12): ramp on the viewport 600→1400px (implemented as 37.5→87.5rem, zoom-coherent); h1+h2 → lg normal, h3(–h6, coordinator assumption) → md semibold, strong → semibold with tuple tracking** → **T20 typestyle landing COMPLETE AND COMMITTED** (`2755440`): tuples+ramp+Inter live in `ds/tokens/typestyles.css`, smoothing in reset, polarity in semantic, rich-text mapped; approved values exact at 600/1000/1400px both schemes, zero unexpected deltas across eight batteries, components change typeface only. **Dark polarity 380/580 user-approved "ok per ora" (provisional — revisitable).** Conventions promoted to **v7** (§5b rewritten around tuples/ramp/consumption-point/polarity/smoothing; §11 admits vendored open fonts — Inter in `ds/fonts/`). **Tuple token naming DECIDED (user, 2026-09-03): `rest|end`** — position-based suffixes applied uniformly (coordinator-executed rename, behavior-neutral, verified live). Next: **C3 rhythm** (findings `typestyle-landing.md` "Anything C3 must know": judge space-2/4/6 gaps against the new line boxes; h1/h2 hierarchy now lives on rhythm alone) and **T21 contract + conformance suite** (absorbs T19's probe + the four confirmed linter candidates; also the frozen rich-text fixture's stale font-role copy cleanup). **THE COMPOSER VISION (user, 2026-09-03) — the project's likely next thesis, brainstorm scheduled:** more than a customizable starter, the user wants **a tool that creates design systems from scratch by composition**: modules (type sets, palettes, components) composed with **tracked dependencies** (fork-on-divergence editing: change rich-text's shared body style and the tool splits the node, no manual style creation), system forking for variant testing (all-sans DS + serif set assigned per component), **no fixed semantic vocabulary** (palette anatomy itself is variable — a 2-colour brutalist must be expressible), **contexts as the primitive** (coloured cards/header sections; components adapt palette/polarity/treatments with no API change; conditional rules like gloss-on-image-cards), driven by a **UI + LLM collaborative loop**, all by composition — explicitly NOT shadcn-style preset piles. Coordinator read (recorded in chat 2026-09-03): coherent maturation of thesis v2 — nothing built is wasted, it inverts roles (protocol v7 = the emitted target language; T21 = compiler tests; the gates corpus = LLM safety knowledge; curation cycles = the tool's UX prototyped manually; M3 derivations = manual fork prototype; the refined core = first reference instance). Honest hard parts: fork-on-divergence requires a MODEL layer owning the graph (CSS becomes deterministic emitted artifact for composed systems — consumer contract unchanged; distinct from the rejected runtime TS descriptor); variable anatomy needs invariants-as-RELATIONS (gates as schema, not token names); contexts generalize the proven re-materialization mechanism. **Brainstorm handoff ready: `docs/tasks/T22-composer-brainstorm.md`** (vision, asset repositioning table, 9-point agenda, user acceptance scenarios, decided constraints incl. the PostCSS-pivot counsel, expected outputs → `findings/composer-brainstorm.md`). **Style-as-entity thread (user concern 2026-09-03, coordinator-probed live, direction proposed — now folds into the composer brainstorm):** the user wants a text style definable WHOLE and applicable as one act (to an h2 or a button) — T20's per-element longhand spray is the mechanical landing form, not the target grammar. Probe results (Chromium): the font shorthand accepts calc() weight + clamp() size/lh from ONE custom property (`font: var(--ds-type-<style>-font)`), tracking rides a second property (letter-spacing is grammatically outside `font` — platform limit); polarity survives via CONTEXT RE-MATERIALIZATION (a context that re-declares the tuple vars re-substitutes the inherited delta — counter-proof confirmed: delta alone does nothing), which pushes polarity toward a protocol attribute (`data-ds-polarity`) and feeds the colour-context study (a context = an attribute that re-declares what depends on it). True single-act application in CSS (@mixin/@apply) is not yet shipped anywhere — the honest platform gap; the tuples become mixins value-unchanged when it lands. Proposed: a behavior-neutral restructure task (two-handle pairs per style + context re-materialization + rich-text two-line mappings + optional `data-ds-type` markup hook per direction §7), batteries as proof. Awaiting the user's nod. Headline confirmed gap: `field.css` descendant `:has(input:…)` state hooks must become anatomical paths (`:has(> [data-part="control"] > input:…)`) — real latent state-reach bug class, untested by any current fixture. The review's font-shorthand caveat is largely superseded by the C1 tuple decision (converging evidence); its `@property` prohibitions, cascade-claim precision, activation/pressed terminology, root-element admissibility matrix, `ds.diagnostics` module idea, and new linter-rule candidates are itemized in the triage.

The linter is **parked, not dropped**: its 30-rule inventory (`docs/findings/m2-synthesis.md` §4–5) stays valid and will be better informed by the mistakes derivations actually surface.
